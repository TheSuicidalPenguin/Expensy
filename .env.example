# Deployment used by `npx convex dev`
CONVEX_DEPLOYMENT=${CONVEX_DEPLOYMENT} # team: Rami, project: expensy
CONVEX_URL=${CONVEX_URL} ## this is for regular operations with db
CONVEX_SITE_URL=${CONVEX_SITE_URL} ## this is for auth

# Exposed to the Vite browser bundle (VITE_ prefix required)
VITE_CONVEX_URL=${VITE_CONVEX_URL}

# Server-only email settings (Convex env vars)
RESEND_API_KEY=${RESEND_API_KEY}
RESEND_FROM=${RESEND_FROM}