{
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then npx convex deploy --cmd 'npm run build'; elif [ -n \"$CONVEX_DEPLOY_KEY\" ]; then npx convex deploy --check-build-environment disable --cmd 'npm run build'; else npm run build; fi"
}
